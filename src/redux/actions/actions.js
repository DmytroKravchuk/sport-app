import {GET_EXERCISE_TYPES, GET_PROGRAMS} from '../types';

/*
export const getExerciseTypes = (exerciseTypes) => {
	return {
		type: GET_EXERCISE_TYPES,
		payload: exerciseTypes
	}
};

export const getPrograms = (programs, exerciseId) => {
	return {
		type: GET_PROGRAMS,
		payload: {
			programs,
			exerciseId
		}
	}
}
*/
